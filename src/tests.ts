// // Testing
// // --------------------------

// import { newMove, applyMove, makeNewGame, getWord } from './state.js';
// import { checkMoveValid } from './validity.js';

// const testGame = makeNewGame({
//   left: ["H", "T", "A"],
//   top: ["R", "P", "I"],
//   right: ["E", "O", "U"],
//   bottom: ["G", "L", "F"],
// });

// // const testMove = newMove([["left", 0], ["top", 2], ["left", 1]]);
// // const testWord = getWord(testGame.board, testMove);
// // const isTestMoveValid = checkMoveValid(testGame, testMove);

// // const nextState = applyMove(testGame, testMove);
// // const nextTestMove = newMove([["left", 1], ["right", 2], ["bottom", 0]]);

// // console.log(checkMoveValid(nextState, nextTestMove))
